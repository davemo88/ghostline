{
  "name": "ghostline",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "cvc": "vite-node scripts/cvc.ts",
    "balance": "vite-node scripts/balance.ts",
    "replay": "vite-node scripts/replay.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.110.0",
    "three": "^0.166.0"
  },
  "devDependencies": {
    "@types/three": "^0.166.0",
    "typescript": "^5.5.0",
    "vite": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
